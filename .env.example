# Local dev: leave unset — chatSocket.js falls back to ws(s)://<current-host>:8787
# Production (Vercel etc.): set to the deployed chat server's wss:// URL
# e.g. VITE_WS_URL=wss://your-app.up.railway.app
VITE_WS_URL=
